NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_API_ROOT_URL=http://localhost:4000
# Public (client-safe) Mapbox token — used only for the Instant Quote's
# satellite measurement map. Get one at https://account.mapbox.com/
# (the default "public" token works; no secret token needed here).
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
